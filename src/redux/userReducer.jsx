const initialData = {
    users: [],
    loggedInUser: {}
}

export const userReducer = (state = initialData, action) => {
    switch (action.type) {
        case 'REGISTER':
           
        case 'LOGIN':

        case 'LOGOUT':
        

        default:
            return state
        

    }
}